# Build artifacts of this standalone, workspace-excluded crate. It is built on
# its own manifest by maturin for the PyPI wheel, so it gets its own target
# rather than sharing the root workspace's. Its lockfile is tracked so
# standalone builds resolve the contracted native binding version;
# scripts/validate_release_metadata.py rejects drift.
/target/
